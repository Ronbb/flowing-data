{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "server debug",
      "cargo": {
        "args": [
          "build",
          "--manifest-path",
          "${workspaceFolder}/server/Cargo.toml"
        ]
      },
      "args": [],
      "cwd": "${workspaceFolder}"
    }
  ]
}
